---
name: good-bundle
description: Use this fixture skill to exercise the validator's bundle branch. It is a minimal but VALID bundle skill whose name equals its directory name. Triggers include 'run the linter test', 'validate the good bundle fixture'.
metadata:
  version: 1.0.0
---

# Good Bundle

A minimal valid bundle-shape skill (`<dir>/SKILL.md`, name == directory name).
Exists only as a known-good fixture for `bin/validate-skills.sh`.
