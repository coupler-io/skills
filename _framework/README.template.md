<!-- ═══════════════════════════════════════════════════════════════════════════
     COUPLER SKILL README TEMPLATE  ·  the SEO / human-discovery surface (axis B)
     Companion to SKILL.template.md. One README ships per skill: {skill}/README.md
     Fill every [BRACKET]. Delete these guidance comments before publishing.
     Convention: _framework/conventions.md § SEO / human-discovery surface (four elements).

     ┌─────────────────────────────────────────────────────────────────────────┐
     │ BOUNDARY: this is a READINESS surface. It states NO reach / traffic       │
     │ metric. It asserts the skill is discoverable by construction — it never   │
     │ claims, promises, or measures reach.                                      │
     └─────────────────────────────────────────────────────────────────────────┘
     ═══════════════════════════════════════════════════════════════════════════ -->

# [Skill Title — the job-to-be-done, source-agnostic]

<!-- ELEMENT 1 · CAPABILITY DESCRIPTION (keyword-optimized, human/crawler-readable) -->
[One or two paragraphs of keyword-optimized prose: what this skill does, for whom (the ICP), and the
senior-analyst outcome it produces over the user's own connected data. Write for humans AND web
crawlers — lead with the capability and the ICP keywords.]

## What it does

<!-- ELEMENT 2 · TRIGGER KEYWORDS (the search / ask phrasings humans & crawlers index on) -->
[List the real search / ask phrasings and ICP keywords a user would type — mirror the trigger phrases
in the skill's `description`. e.g.:]
- [trigger phrase 1 — e.g. "how are my ads performing"]
- [trigger phrase 2 — e.g. "weekly PPC report"]
- [trigger phrase 3 — e.g. "why did CPA spike"]
- [ICP keyword cluster — e.g. "Facebook + Google Ads efficiency, ROAS, CPA, blended CAC"]

## Tool Integrations & Related Skills

<!-- ELEMENT 3 · TOOL INTEGRATIONS + RELATED SKILLS -->
**Tool integrations:** [the Coupler.io integrations / MCP tools this skill uses — e.g. Facebook Ads,
Google Ads via `get-data`, `get-schema`, `list-dataflows`.]

**Related skills:**
- [`reporting/report-generation` — composed at the Present step for report formatting + validation.]
- [`create-dataflow` — the core connect skill this cross-references for the heavy source-connection flow.]
- [`generate-data-set-context` — the core context-enrichment skill for the enrich path.]
- [sibling analytical skills this hands off to or complements.]

## Install

<!-- ELEMENT 4 · INSTALL METHOD -->
[How a human installs / invokes this skill. e.g.:]
- **In-product (existing Coupler users):** connect the Coupler.io MCP; the skill is discoverable via
  `list-skills` and loaded via `get-skill`.
- **Claude Code plugin / marketplace:** [install command / marketplace link if listed.]
- **Community track (Track A):** [link to the `marketingskills` PR / listing if applicable.]

## Site page guidance (GEO/SEO — for the coupler.io skills page)

<!-- ELEMENT 5 · SITE PAGE GUIDANCE
     Fills acquisition-readiness checklist items (v) GEO/SEO-ready and (vi) GTM attribution label,
     so they have a template home instead of being hand-rolled per skill. Readiness by construction —
     states NO reach/traffic metric. -->
- **Suggested slug:** [ICP-keyworded page slug — e.g. `coupler.io/skills/ppc-analytics`
  (alt: `/skills/facebook-google-ads-analysis`)]
- **Suggested title:** [ICP-keyworded page title — lead with the job-to-be-done + the ICP keywords,
  e.g. "PPC Analytics AI Skill — Facebook & Google Ads performance, ROAS and CPA analysis for
  marketing agencies and eCommerce"]
- **Comparison / switch angle:** [a "vs …" / "switching from …" angle a not-yet-user searches on —
  e.g. vs. native Ads Manager reports and manual spreadsheet reporting; switching from Supermetrics-style
  manual exports.]
- **GTM attribution label (page-action-block):** [`<page>-<action>-<block>` where `<page>` matches this
  skill's `name` — e.g. `ppc-analytics-download-skill-hero` — so the skill-page/button → app click is
  GA4-trackable.]
